const path = require('path')

module.exports = {
    config: path.resolve(__dirname, 'src', 'config', 'database.js'), //cada virgula eh uma pasta  a mais
    'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations')
}